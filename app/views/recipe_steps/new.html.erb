<%= form_for [@recipe, @recipe_step] do |f| %>
  <%= f.label :body %>
  <%= f.text_field :body %>

  <%= f.submit 'Add This Step' %>
<% end %>

<%= link_to 'ALL DONE', @recipe, class: 'btn btn-lg btn-default' %>
